<form method="post" action="{{ path('app_market_subscription_request_delete', {'id': market_subscription_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ market_subscription_request.id) }}">
    <button class="btn">Delete</button>
</form>
