{% extends 'base.html.twig' %}

{% block title %}Api{% endblock %}

{% block body %}
    <h1>Api</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ api.id }}</td>
            </tr>
            <tr>
                <th>BaseUrl</th>
                <td>{{ api.baseUrl }}</td>
            </tr>
            <tr>
                <th>ApiKeyValue</th>
                <td>{{ api.apiKeyValue }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ api.login }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ api.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_api_index') }}">back to list</a>

    <a href="{{ path('app_api_edit', {'id': api.id}) }}">edit</a>

    {{ include('api/_delete_form.html.twig') }}
{% endblock %}
