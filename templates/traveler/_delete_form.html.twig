<form method="post" action="{{ path('app_traveler_delete', {'id': traveler.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ traveler.id) }}">
    <button class="btn">Delete</button>
</form>
