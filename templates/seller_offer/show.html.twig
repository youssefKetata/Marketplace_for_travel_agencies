{% extends 'base.html.twig' %}

{% block title %}SellerOffer{% endblock %}

{% block body %}
    <h1>SellerOffer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seller_offer.id }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ seller_offer.creationDate ? seller_offer.creationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ seller_offer.startDate ? seller_offer.startDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_seller_offer_index') }}">back to list</a>

    <a href="{{ path('app_seller_offer_edit', {'id': seller_offer.id}) }}">edit</a>

    {{ include('seller_offer/_delete_form.html.twig') }}
{% endblock %}
