<form method="post" action="{{ path('app_api_product_delete', {'id': api_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ api_product.id) }}">
    <button class="btn">Delete</button>
</form>
