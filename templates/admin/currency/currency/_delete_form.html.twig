<form method="post" action="{{ path('app_admin_currency_currency_delete', {'code': currency.code}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ currency.code) }}">
    <button class="btn">Delete</button>
</form>
