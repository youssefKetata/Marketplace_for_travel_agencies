
{% extends 'admin/base_admin.html.twig' %}

{% block title %}New MarketSubscriptionRequest{% endblock %}

{% block container %}
    <br>
    <br>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">

                    <div class="card-body">
                        {{ include('market_subscription_request/_form.html.twig') }}
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('app_market_subscription_request_index') }}" class="btn btn-secondary"> <i class="fas fa-arrow-left"> </i> Back to list</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
