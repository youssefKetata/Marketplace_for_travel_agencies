<form method="post" action="{{ path('app_admin_location_country_delete', {'code': country.code}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ country.code) }}">
    <button class="btn">Delete</button>
</form>
