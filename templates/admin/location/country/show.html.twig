{% extends 'base.html.twig' %}

{% block title %}Country{% endblock %}

{% block body %}
    {#    <h1>Country</h1> #}

    <table class="table">
        <tbody>
        <tr>
            <th>Code</th>
            <td>{{ country.code }}</td>
        </tr>
        <tr>
            <th>Alpha3</th>
            <td>{{ country.alpha3 }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ country.name }}</td>
        </tr>
        <tr>
            <th>Phone_code</th>
            <td>{{ country.phoneCode }}</td>
        </tr>
        <tr>
            <th>Capital</th>
            <td>{{ country.capital }}</td>
        </tr>
        <tr>
            <th>Active</th>
            <td>{{ country.active ? 'Yes' : 'No' }}</td>
        </tr>
        </tbody>
    </table>

    {#    <a href="{{ path('app_admin_location_country_index') }}">back to list</a> #}

    {#    <a href="{{ path('app_admin_location_country_edit', {'code': country.code}) }}">edit</a> #}

    {#    {{ include('admin/location/country/_delete_form.html.twig') }} #}
{% endblock %}
