{% extends 'base.html.twig' %}

{% block title %}ApiProduct{% endblock %}

{% block body %}
    <h1>ApiProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ api_product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ api_product.name }}</td>
            </tr>
            <tr>
                <th>IdProductFromApi</th>
                <td>{{ api_product.idProductFromApi }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_api_product_index') }}">back to list</a>

    <a href="{{ path('app_api_product_edit', {'id': api_product.id}) }}">edit</a>

    {{ include('api_product/_delete_form.html.twig') }}
{% endblock %}
