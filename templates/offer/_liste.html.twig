{% for offer in offers %}
    <tr>

        <div class="d-flex align-items-center">

            <td>{{ offer.id }}</td>
            <td>{{ offer.name }}</td>
            <td>{{ offer.nbProductTypes }}</td>
            <td>{{ offer.nbDays }}</td>
            <td>
                <button
                        class="btn btn-link btn-rounded btn-sm fw-bold btn-info btn-sm"
                        data-action="modal-form#openModal"
                        data-form-url-value="{{ path('app_offer_show', {'id': offer.id}) }}"
                        data-node-dest-id-value="myOneCustomContent"
                        data-refresh-key-value="refresh-html">
                    <i class="fas fa-eye"></i> View
                </button>
{#                <button#}
{#                        class=" btn btn-link btn-rounded btn-sm fw-bold btn-success btn-sm"#}
{#                        data-action="modal-form#openModal"#}
{#                        data-form-url-value="{{ path('app_offer_edit', {'id': offer.id}) }}"#}

{#                        data-node-dest-id-value="myOneCustomContent"#}
{#                        data-refresh-key-value="refresh-html">#}
{#                    <i class="fas fa-edit"></i> Edit#}
{#                </button>#}
                <a class=" btn btn-link btn-rounded btn-sm fw-bold btn-success btn-sm" href="{{ path('app_offer_edit', {'id': offer.id}) }}">Edit</a>

                <button
                        class="btn btn-link btn-rounded btn-sm fw-bold btn-danger btn-sm"
                        data-action="modal-form#openModal"
                        data-form-url-value="{{ path('app_offer_delete', {'id': offer.id}) }}"
                        data-node-dest-id-value="myOneCustomContent"
                        data-refresh-key-value="refresh-html">
                    <i class="fas fa-trash-alt"></i> DELETE
                </button>


                <a class="btn btn-info btn-sm" href="{{ path('app_offerProductTypes', {'id': offer.id}) }}">View Offres</a>

            </td>

        </div>
    </tr>

    </td>
    </tr>
{% else %}
    <tr>
        <td colspan="3">no records found</td>
    </tr>
{% endfor %}