 {% for traveler in nonSellers %}
    <tr>
        <div class="d-flex align-items-center">

            <td>{{ traveler.id }}</td>
            <td>{{ traveler.firstName }}</td>
            <td>{{ traveler.lastName }}</td>
            <td>{{ traveler.address }}</td>

            <td>
                <button
                        class="btn btn-link btn-rounded btn-sm fw-bold btn-info btn-sm"
                        data-action="modal-form#openModal"
                        data-form-url-value="{{ path('app_traveler_show', {'id': traveler.id}) }}"
                        data-node-dest-id-value="myOneCustomContent"
                        data-refresh-key-value="refresh-html">
                    <i class="fas fa-eye"></i> View
                </button>


                <button
                        class=" btn btn-link btn-rounded btn-sm fw-bold btn-success btn-sm"
                        data-action="modal-form#openModal"
                        data-form-url-value="{{ path('app_traveler_edit', {'id': traveler.id}) }}"
                        data-url-value="{{ path('app_traveler_index', { ajax: 1 } ) }}"
                        data-node-dest-id-value="myOneCustomContent"
                        data-refresh-key-value="refresh-html">
                    <i class="fas fa-edit"></i> Edit
                </button>
                <button
                        class="btn btn-link btn-rounded btn-sm fw-bold btn-danger btn-sm"
                        data-action="modal-form#openModal"
                        data-form-url-value="{{ path('app_traveler_delete', {'id': traveler.id}) }}"
                        {#                        data-url-value="{{ path('app_product_type_index', { ajax: 1 } ) }}" #}
                        data-node-dest-id-value="myOneCustomContent"
                        data-refresh-key-value="refresh-html">
                    <i class="fas fa-trash-alt"></i> Delete
                </button>


            </td>
        </div>
    </tr>

{% else %}
    <tr>
        <td colspan="3">no records found</td>
    </tr>

{% endfor %}